[Unit]
Description=Check for PCIe devices with vendor ID 1f9d and reboot once if not found
After=network.target data.mount
Requires=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/check_pcie_device.sh
#RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
